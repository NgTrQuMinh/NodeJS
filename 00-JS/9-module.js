// Bài 9: Module (require/export) – mô phỏng
// Yêu cầu (làm việc với file):

// Tạo file math.js export các hàm: add(a,b), subtract(a,b), multiply(a,b).

// Tạo file app.js require các hàm trên và sử dụng để tính (3 + 4) * 2.
// (Nếu chưa dùng Node, bạn có thể viết code giả lập bằng object module.)